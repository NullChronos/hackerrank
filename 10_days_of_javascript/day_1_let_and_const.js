function main() {
  const PI = Math.PI
  const r = readLine()

  let a = PI * Math.pow(r, 2)
  let p = 2 * PI * r

  // Print the area of the circle:
  console.log(a)
  // Print the perimeter of the circle:
  console.log(p)
}
